$(document).ready(function () {
	$("#formAddGuru").submit(function () {

		// Tampilkan overlay
		$("#overlayGuru").removeClass("hidden");

	});


	$("#formAddKelas").submit(function () {

		// Tampilkan overlay
		$("#overlayKelas").removeClass("hidden");

	});


	$("#formEditMapel").submit(function () {

		// Tampilkan overlay
		$("#overlayEditMapel").removeClass("hidden");

	});


	$("#formAddMapel").submit(function () {

		// Tampilkan overlay
		$("#overlayAddMapel").removeClass("hidden");

	});


	$("#formAddJadwalKhusus").submit(function () {

		// Tampilkan overlay
		$("#overlayJadwalKhusus").removeClass("hidden");

	});


	$("#formBuatJadwalku").submit(function () {

		// Tampilkan overlay
		$("#overlayBuatJadwalku").removeClass("hidden");

	});



	$("#formPlotingJadwal").submit(function () {

		// Tampilkan overlay
		$("#overlayPlotingJadwal").removeClass("hidden");

	});

	$("#formExportJadwal").submit(function () {

		// Tampilkan overlay
		$("#overlayExportJadwal").removeClass("hidden");

	});

	$("#formResetJadwal").submit(function () {

		// Tampilkan overlay
		$("#overlayResetJadwal").removeClass("hidden");

	});

	$("#formResetJadwalKu").submit(function () {

		// Tampilkan overlay
		$("#overlayResetJadwalKu").removeClass("hidden");

	});


	$("#formUpdateKelas").submit(function () {

		// Tampilkan overlay
		$("#overlayKelasEdit").removeClass("hidden");

	});


	$("#formUpdateAturan").submit(function () {

		// Tampilkan overlay
		$("#overlayAturanSesi").removeClass("hidden");

	});


	$("#formFinalShare").submit(function () {

		// Tampilkan overlay
		$("#overlayFinalShare").removeClass("hidden");

	});


	$("#formAddTahunAkademik").submit(function () {

		// Tampilkan overlay
		$("#overlayTahunAkademik").removeClass("hidden");

	});

});
